#include <iostream>
#include <fstream>
#include <string>
#include <ctime>
#include <stdio.h>
#include <math.h>
#include <unistd.h>
#include <time.h>
#include <sys/time.h> 
#include <stdlib.h> 
#include <iomanip>
#include "command.h"
using namespace std;

int main(int argv,char* argc[]){
int arg=atoi(argc[1]);
double voltage=float(atof(argc[2]));
set_threshold(board, ch, voltage);

cout<<"done"<<endl;

return 0;
}
